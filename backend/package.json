{
  "name": "ecomnext-backend",
  "version": "0.1.0",
  "private": true,
  "scripts": {
    "start": "json-server --watch ./db.json --port 3001",
     "dev": "next dev",
    "build": "next build",
    "lint": "eslint"
  },
  "dependencies": {
    "json-server": "^1.0.0-beta.9"
  }
}
